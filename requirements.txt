# Requirements untuk skripsi "Strategi Dekomposisi Pipeline Berbasis Konversi
# Modalitas Menggunakan Gemma-3 dan RoBERTa" (Gultom, 2026).
#
# Hanya berisi dependensi yang BENAR-BENAR di-import oleh notebook pada repo.
# Untuk replikasi lingkungan penelitian (Python 3.10.19 + Conda 25.9.1 + Intel
# Arc / PyTorch XPU), gunakan environment.yml.
#
# =============================================================================
# CATATAN PyTorch backend
# =============================================================================
# Versi yang dipakai di penelitian: torch==2.10.0+xpu (Intel Arc Graphics).
# Wheel +xpu hanya tersedia di index khusus PyTorch. Pilih SALAH SATU:
#
#   a) Intel XPU (sesuai naskah):
#        pip install --extra-index-url https://download.pytorch.org/whl/xpu \
#            torch==2.10.0
#
#   b) NVIDIA CUDA 12.x:
#        pip install --extra-index-url https://download.pytorch.org/whl/cu121 \
#            torch==2.10.0
#
#   c) CPU only:
#        pip install torch==2.10.0
#
# Setelah PyTorch ter-install dengan backend yang tepat, install sisa dependency
# di bawah ini dengan: pip install -r requirements.txt
# =============================================================================

# Deep learning & transformer
transformers==5.0.1.dev0

# Multimodal LLM API (ekstraksi deskripsi visual Gemma-3)
google-generativeai==0.8.6

# Evaluasi keselarasan semantik
bert-score==0.3.13

# Data & komputasi numerik
pandas==2.3.3
numpy==2.2.6
scikit-learn==1.7.2
scipy==1.15.3

# I/O gambar & utilitas notebook
pillow==12.0.0
tqdm==4.67.1
tabulate==0.10.0

# Jupyter kernel
ipykernel==7.2.0
